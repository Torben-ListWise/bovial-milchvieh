Der Testlauf zeigt ein Problem, das mein ursprünglicher Patch A nicht lösen kann: Turn 0 wird laut Abschnitt 2 bei jeder neuen Frage hart über tool_choice = {type: "tool", name: "get_schema"} erzwungen — das ist eine Entscheidung der Orchestrierungs-Code-Ebene, nicht des Modells. Ein Satz im System-Prompt wie "rufe get_schema nicht erneut auf" hat darauf keinen Einfluss, weil Claude an dieser Stelle gar keine Wahl hat, ob es den Tool-Call macht. Die Formulierung "Was der Audit-Patch A konkret verbessert hätte" im Dokument bestätigt das implizit (Konjunktiv II = noch nicht umgesetzt).
[Sicher] Patch A muss als Code-Änderung in der Turn-0-Logik landen, nicht als Prompt-Regel. Konkret:
if (session.hasSchemaCache && session.turnsSinceSchema <= 5 && session.datasetId === currentDatasetId) {
  toolChoice = { type: "auto" };       // get_schema wird optional, nicht erzwungen
} else {
  toolChoice = { type: "tool", name: "get_schema" };
}
Ich widerspreche außerdem meinem eigenen Patch D, weil der Testlauf zeigt, dass er an der Realität vorbeigeht: Im Beispiel bekommen die Zahlenwerte aus search_knowledge (200 Tsd./ml, 2–4 ct/kg) das [2]-Format — nicht nur Werte aus DB/Dokument, wie ich es eingeschränkt hatte. Das deckt sich mit der ursprünglichen Regel ("Zahlen aus Werkzeug-Ergebnissen", nicht nur DB-Werkzeugen). Das würde ich stattdessen tun: [n] gilt für jeden quantitativen Wert aus jedem Tool inklusive search_knowledge; der kursive Vertrauensgrad-Tag (*[Bibliothek]* etc.) markiert dagegen die gesamte Einschätzung/Empfehlung am Satz- oder Absatzende, nicht einzelne Zahlen. Das Risiko des ursprünglichen Patch D: Bei wörtlicher Anwendung hätte der Agent die Bibliothekswerte fälschlich ohne [n]-Marker ausgegeben — genau die Inkonsistenz, die Patch D eigentlich verhindern sollte.
Zusätzlicher Fund: Die Eskalationsregel (Patch F, Investitionsschwelle) sollte sich auf das Ergebnis von calculate_investment beziehen, nicht auf eine vom Nutzer genannte Zahl — sonst greift sie nicht, wenn der Investitionsbetrag erst während der Analyse berechnet wird.
Echte Baseline aus diesem Run (für Patch G, Metrik 2): 5 Tool-Calls, ~3.000 Token, ~10 Sek. Gesamtlaufzeit für eine mittelkomplexe Frage mit Diagramm. Das trage ich als ersten Messwert nach.
Ich aktualisiere das Dokument entsprechend.5 Dateien bearbeitet, datei lesen5 Dateien bearbeitet, datei lesen[Wahrscheinlich] Runde 6 braucht einen Testfall mit dokumentAvailable: false, um Patch B gegen den bestehenden Grounding-Check abzugleichen — dieser Fall fehlt im aktuellen Testlauf komplett. Hast du einen zweiten Replit-Testlauf ohne Datenquelle, oder soll ich aus der Architekturbeschreibung eine Hypothese ableiten, die du dann verifizierst?
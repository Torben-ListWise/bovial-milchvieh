Patch G darf nicht in SYSTEM_PROMPT_BASE landen. Das ist exakt das, was wir vor zwei Runden in der Klarstellung ausgeschlossen haben: Die 5 Loop-Metriken sind für unseren externen Audit-Prozess gedacht, nicht für das Modell zur Laufzeit. Wenn dieser Text im Produktions-Prompt steht, bezahlt jede einzelne Bauernfrage für 5 Sätze, die keine Funktion haben — das Modell kann "Eskalationsquote über Sessions hinweg" ohnehin nicht aus einem einzelnen System-Prompt heraus tracken. Aus Phase 1 entfernen, bleibt nur unser eigenes Protokoll.
[Sicher] — gelöst, keine Aktion nötig: Die Patch-D-Entscheidung (auch search_web bekommt [n]) löst die in Runde 8 eskalierte offene Frage auf. Schließe ich im Protokoll als entschieden ab.
[Sicher] — Phase 1, Patch F+L: Abweichung von meinem Vorschlag, ohne dass das explizit entschieden wurde. Mein Patch M (Runde 9) sah vor: nur eskalieren, wenn basis.rowCount bei beiden Werkzeugen identisch ist; bei unterschiedlichem rowCount nur transparent erklären, nicht eskalieren. Task #220 hat diese Differenzierung nicht übernommen — es eskaliert bei jeder >10%-Abweichung, unabhängig vom rowCount. Das ist genau der Fall aus Testlauf v5 (14.230 vs. 11.840 Zeilen). Ist das eine bewusste Vereinfachung (im Zweifel eskalieren ist konservativer), oder ein Versehen?
[Sicher] — Phase 2, zwei Punkte vor dem Deploy klären:

Der Scan nutzt messages.slice(-10), aber die vorausgehende Bedingung prüft opts.conversation.length !== 1. Wenn messages und opts.conversation dasselbe, pro Frage zurückgesetzte Array sind, findet der Scan nie ein Ergebnis aus einer früheren Frage — Patch H wäre dann wirkungslose Totlogik, die nie über den ersten Check hinauskommt. Das muss vor dem Deploy verifiziert werden, nicht nach.
Das Erkennungsmerkmal content.includes("rowCount") ist zu unscharf: Laut allen Testprotokollen (v1–v5) enthalten auch get_timeseries, get_metric_stats und get_group_aggregate ein basis.rowCount-Feld — der Scan würde also auch bei diesen Tools fälschlich "schemaFound" melden, nicht nur bei get_schema.

[Sicher] — Phase 3: Sieht solide aus, Cache korrekt pro Turn zurückgesetzt. Einziger Punkt bleibt die schon dokumentierte offene Frage zur Parameter-Match-Strenge.
[Sicher] — Phase 4, neuer Befund aus Anthropics aktueller Doku (gerade verifiziert, nicht aus dem Gedächtnis): Eine Änderung von tool_choice invalidiert den Cache. Turn 0 (forced) → Turn 1 (auto) ist aber genau so eine Änderung — bei jeder einzelnen Frage, unabhängig von Patch H oder der Wartezeit aus Befund 23. Das heißt: Der Cache-Schreibvorgang aus Turn 0 wird in Turn 1 nicht gelesen, sondern neu geschrieben (zu 1,25× Preis, nicht 1×) — der eigentliche Spareffekt entsteht erst von Turn 1 auf Turn 2/3 (dort bleibt tool_choice: "auto" stabil). Meine bisherige Schätzung der Ersparnis war dadurch zu hoch angesetzt. Interessante Konsequenz: Es könnte cache-freundlicher sein, tool_choice nie zu erzwingen (immer "auto", Schema-Pflicht nur über die Prompt-Regel + den bereits vorhandenen Grounding-Check-Fallback absichern) — das würde Patch H stark vereinfachen und Patch J ungestört lassen. Das ist ein Vorschlag, keine Entscheidung, die ich für dich treffen sollte.
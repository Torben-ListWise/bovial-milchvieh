Patch B funktioniert endlich nachweislich — aber genau dieser Test deckt eine Lücke eine Ebene darüber auf, die bisher niemand getestet hat: den Teilzustand zwischen "alles da" und "nichts da". In Szenario 4a sind Betriebsdaten vorhanden, aber kein Dokument — und der Hinweis darauf im Antworttext kommt laut Protokoll nur, weil Claude das selbst bemerkt, nicht weil eine Regel das vorschreibt. Das ist nicht garantiert reproduzierbar.
[Sicher] Bestätigt, keine Änderung: Patch B greift exakt wie spezifiziert (0 Felder + kein Dokument → DB-/Dokument-Tools gesperrt, nur search_web mit *[Web]*). Patch D-Zitierlogik hält auch im vierten unabhängigen Lauf ([1] an get_timeseries-Werten, [2] an search_knowledge-Werten).
Patch K — Pflicht-Transparenz bei partieller Datengrundlage (neu)

Wenn dokumentAvailable: false UND fields.length > 0 (Betriebsdaten vorhanden, aber kein hochgeladenes Dokument): Antwort MUSS einen kurzen Abschnitt „Hinweis zur Datengrundlage" enthalten — Analyse basiert nur auf Betriebsdaten + Wissensbibliothek, kein eigenes Dokument vorhanden, mit Upload-Vorschlag. Nicht optional/heuristisch, sondern verbindliche Regel — analog zu Patch B, aber für den Teilzustand statt den Komplett-Leer-Zustand.

[Schätzung] — Eskalation, kein Auto-Fix: In Szenario 4b bekommt die Web-Zahl (8.600 kg) nur *[Web]*, kein [n]. Das widerspricht wörtlich der Patch-D-Formulierung „jedem Tool, inklusive search_knowledge" — wenn das wörtlich gilt, müsste auch search_web einen [n] bekommen. Da das Citations-Array laut früherem Protokoll bereits ein sourceType-Feld kennt, könnte das technisch absichtlich getrennt sein (intern/grounded = nummeriert, extern = nur Vertrauens-Tag ohne Nummer). Das ist eine Design-Entscheidung, keine, die ich für dich treffen sollte.
Kein Abbruch der Loop — diese Runde hat zwei echte neue Befunde produziert, also griffe Patch Gs Abbruchkriterium hier nicht.3 Dateien bearbeitet, datei lesen3 Dateien bearbeitet, datei lesen[Wahrscheinlich] Beide ursprünglich aus dem Regelwerk identifizierten ungetesteten Pfade (Patch B, widersprüchliche Werte zwischen zwei Tools) sind jetzt zur Hälfte abgedeckt — der Konflikt-Fall zwischen zwei Werkzeugen (Eskalationstrigger 1) ist immer noch in keinem der fünf Testläufe vorgekommen. Das wäre mein Vorschlag für den nächsten gezielten Testfall, statt eines weiteren Erfolgsszenarios.